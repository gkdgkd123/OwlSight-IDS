[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (may need Redis)
    slow: Slow tests (model loading, API calls)

addopts =
    -v
    --tb=short
    -x
    --strict-markers
